# reports.core web surface

The first reports.core release exposes backend APIs and command handlers only.

Future UI components should provide a shared export button and artifact download panel that call `/api/reports` instead of embedding per-app export logic.

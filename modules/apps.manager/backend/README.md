# Apps Manager Backend

`apps.manager` is the required control module. Its current backend behavior is kernel-hosted because it is the bootstrap module that reads module manifests and manages lifecycle operations.

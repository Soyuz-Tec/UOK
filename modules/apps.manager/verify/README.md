# Apps Manager Verification

`UokCandidateAppsManager.ps1` is the module-owned release verifier declared by the manifest. Runtime and release verification assets live here rather than under `tests/` so candidate images can exclude test source.

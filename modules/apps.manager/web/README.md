# Apps Manager Web

The Apps Manager UI is currently implemented in `web/src/features/apps` and is governed by this module manifest. It is a required bootstrap control surface, so it remains part of the shared shell rather than an optional module surface registry entry.

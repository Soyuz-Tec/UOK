# Planning Core Verification

The PowerShell files in this directory compose the Planning candidate scenario. `runtime/` contains persistent PostgreSQL, CPM, replay, and outbox verification assets used by Planning release-readiness operations; none are pytest discovery inputs.

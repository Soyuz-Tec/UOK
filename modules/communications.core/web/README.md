# communications.core web surface

The compiled K Connect thread workspace is owned under
`web/src/features/communications` and registered through the frontend module
surface registry. This module-local directory records manifest ownership until
runtime-loaded frontend module bundles are introduced.

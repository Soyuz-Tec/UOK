# Communications Core Verification

This directory owns the manifest-declared Communications candidate verifier
used by the generic UOK release catalog. It proves exact create/read identity,
role denial, retained Delete/Restore, Archived discovery, validator continuity
across exact reloads, cleanup through a final ETag-guarded recoverable Delete,
and disabled-provider behavior.

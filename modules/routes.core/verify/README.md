# routes.core candidate verification

`UokCandidateRouteCorridor.ps1` proves dependency installation, tenant-authenticated
Location resolution, ordered Route creation/update, history, lifecycle filtering,
restore, and disabled-module fail-closed behavior against the local candidate.

# routes.core tests

Python tests cover Route/Corridor domain rules, owner lifecycle behavior,
Location facade consumption, audit evidence, and tenant isolation. Frontend
tests live only under `modules/routes.core/tests/web`.

# Contacts Core Web

The Contacts UI is currently implemented in `web/src/features/contacts`. The next packaging step is moving module-specific UI behind this module root while keeping shared shell components in `web/src`.

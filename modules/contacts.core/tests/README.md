# Contacts Core Tests

Contacts behavior pytest suites live in this module-owned test directory and are included in the full UOK verification gate through `pyproject.toml` and GitHub CI file-by-file discovery.

The Contacts candidate verifier scenario lives under `modules/contacts.core/tests/verify` and is discovered from `modules/contacts.core/manifest.yaml`.

# Contacts Core Tests

The Contacts candidate verifier scenario now lives under `modules/contacts.core/tests/verify` and is discovered from `modules/contacts.core/manifest.yaml`.

Contacts behavior pytest suites currently live in top-level `tests/`. Future module packaging should allow those tests to move here without losing the full UOK verification gate.

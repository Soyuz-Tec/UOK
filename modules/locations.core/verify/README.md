# Location Master Verification

**Status:** Active module-owned candidate verification.

**Current candidate:** `UOK-3.1.0-alpha.3`

`UokCandidateLocationMaster.ps1` owns the manifest-declared runtime scenario for install, command/audit correlation, actor-visible reads, canonical-name history, archive filtering, restore, and disabled-module fail-closed behavior.

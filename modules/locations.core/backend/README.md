# Location Master Backend

**Status:** Active private module backend.

**Current candidate:** `UOK-3.1.0-alpha.3`

The supported runtime-composition surface is `uok_locations_core.public_api`. Validation, HTTP delivery, commands, policy, service behavior, and SQLAlchemy mappings remain private under `_internal`.
